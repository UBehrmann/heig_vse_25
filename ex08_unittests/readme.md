
# build

To build and run the unit tests for the floating point operations, follow these steps:

```bash
cd /home/baer/Desktop/vmShare/vse25_student/ex08_unittests/code
mkdir -p build
cd build
cmake ..
make
./VSE_unittests
```